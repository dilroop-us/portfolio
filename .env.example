# Digital Ocean Managed PostgreSQL Database (Production)
# Get your connection string from: https://cloud.digitalocean.com/databases
# Format: postgresql://doadmin:password@cluster-do-user-XXX.db.ondigitalocean.com:25060/defaultdb?sslmode=require
DATABASE_URL=postgresql://doadmin:your_password@cluster-do-user-123-0.db.ondigitalocean.com:25060/defaultdb?sslmode=require

# Redis Configuration (Docker container for local, or cloud for production)
REDIS_URL=redis://localhost:6379

# Application Secrets
SESSION_SECRET=your-super-secret-key-change-this-in-production-min-32-chars

# Email/SMTP Configuration (for contact form notifications)
# For Gmail: Use your Gmail address and create an App Password at https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_FROM=noreply@yourdomain.com
MAIL_FROM_NAME=Portfolio Contact Form
MAIL_TO=your-email@gmail.com
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_STARTTLS=true
MAIL_SSL_TLS=false
MAIL_USE_CREDENTIALS=true

# Notes:
# - Digital Ocean databases use port 25060 (not standard 5432)
# - SSL is always required (sslmode=require)
# - Remember to whitelist your IP in Digital Ocean's "Trusted Sources"
# - For Docker: Use the same DATABASE_URL (no local PostgreSQL needed)
